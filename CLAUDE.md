# 小助手

这是一个使用Flutter为自己设计的助手类App，包名 `assistant`，每个功能各自独立。

## 技术栈

- Flutter + Riverpod 状态管理
- GoRouter 路由
- Drift (SQLite) 数据库，当前 schema v4
- 中文日期本地化 (intl)

## 功能

### 随手记
路径: `/journal`
- 快速记录简短消息，支持8种心情（愤怒/难过/悲伤/低落/普通/开心/欣喜/高兴）
- 底部输入栏左侧点击情绪图标弹出弹窗选择，输入内容和当前情绪同行
- 发送后不重置情绪，保持上次选中状态
- 搜索页（`/journal/search`）改为按日期分组浏览，支持日期选择器跳转
- 设置页（`/journal/settings`）支持导出指定日期范围的 JSON 数据
- 卡片时间显示：1分钟内显示"刚刚"，当天显示 HH:mm，同一年显示 M月d日，往年显示 yyyy年M月d日
- EntryCard 每30秒自动刷新时间显示

### 资产
路径: `/assets`
- 列出各项资产及金额，顶部显示总额
- 支持5种类型：现金、固收、权益、商品、其他
- 资产字段：名称、类型、账户（可选）、金额、备注
- 账户字段用于区分资产所在位置（支付宝、微信、银行卡等）
- 导出 JSON 包含 type/account 字段，便于 AI 分析
- 数据库 schema v4，从空库创建或从旧版本迁移